# venv for the synthetic-data pipeline (kept separate from the system torch
# used by kt/): python3 -m venv .venv-vllm && .venv-vllm/bin/pip install -r synth/requirements.txt
#
# Pins matter on this machine (NVIDIA driver = CUDA 12.8):
# - vllm>=0.25 pulls a CUDA-13 torch build -> "driver too old" at engine start
# - vllm 0.11.2 pairs with torch 2.9.0+cu128
# - flashinfer-cubin must match flashinfer (0.5.2); a newer cubin left over
#   from a vllm upgrade breaks engine startup
# - torchcodec must NOT be installed (its ffmpeg-7 native lib fails to load
#   against system ffmpeg 4.2; vllm imports it only when present)
vllm==0.11.2
flashinfer-cubin==0.5.2
